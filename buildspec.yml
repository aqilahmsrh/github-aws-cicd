version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.8
   build:
     commands:
      - aws lambda update-function-code --function-name lambda-yt-777 --s3-bucket bucket-test-777 --s3-key aqilahmsrh.zip
